def container_id(key):
    return '{}_container'.format(key)


def drop_id(key):
    return '{}_drop_down'.format(key)


def collapse_container_id(key):
    return '{}_collapse_container'.format(key)


def collapse_button_id(key):
    return '{}_collapse_button'.format(key)


def collapse_id(key):
    return '{}_collapse'.format(key)
